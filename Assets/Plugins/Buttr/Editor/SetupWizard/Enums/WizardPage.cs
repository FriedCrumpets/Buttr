namespace Buttr.Editor.SetupWizard {
    /// <summary>
    /// The top-level page the wizard is currently displaying.
    /// </summary>
    internal enum WizardPage {
        Selection = 0,
        Installing = 1,
        CustomSetup = 2,
    }
}