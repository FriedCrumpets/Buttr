namespace Buttr.Editor.SetupWizard {
    /// <summary>
    /// The sub-tabs within the Custom Setup page (Window 2).
    /// </summary>
    internal enum CustomSetupTab {
        Structure = 0,
        Boot = 1,
        Persistence = 2
    }
}